/** @type {import('next').NextConfig} */
const nextConfig = {
	trailingSlash: true
}

// module.exports = {...nextConfig, experimental: {
// 	esmExternals: 'loose'
// }}

module.exports = nextConfig;